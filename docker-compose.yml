services:
  iperf-runner:
    build: .
    image: iperf-stress-test:latest
    container_name: iperf-stress-test
    ports:
      - "8800:8800"
    volumes:
      - ./data:/data
    restart: unless-stopped
    # Uncomment if firewalls are reachable only from the host network:
    # network_mode: host
